- name: Install Python prerequisites
  command: | 
    pip3.10 install \
      graphviz \
      dask[complete] \
      jupyterlab \
      dask-labextension \
      dask-jobqueue \
      memory_profiler \
      coiled
  register: cmdln
  check_mode: no